# ChatGPT Project Source Pack

Current as of 2026-07-26. Current document count: 20.

Upload all Markdown files in this directory to the ChatGPT Project. The pack is organized by current subsystem truth, not by development milestone. Do not upload `ai-knowledge/raw-chats`, the append-only `project-control` ledgers, ignored session/build folders, or historical branch artifacts as project knowledge.

Authority order:

1. Current code and `package.json` for implemented behavior.
2. `AGENTS.md` and `CLAUDE.md` for repository constraints.
3. `01_CURRENT_TRUTH.md` and `03_SYSTEM_ARCHITECTURE.md`.
4. Topic documents in this directory.
5. `project-control/` only for evidence, runbooks, and historical traceability.

When evidence conflicts, do not average the claims. Prefer code for implemented behavior, a fresh git fetch for repository synchronization, and dated operator-approved evidence for production/provider state.

Keep this pack at or below the ChatGPT Project upload limit: do not exceed 20 documents. Update an existing topic file instead of adding a milestone file.
