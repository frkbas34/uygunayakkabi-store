# DEPLOYMENT LOG — Uygunayakkabi

_Created 2026-06-14. Newest first. Each deploy = fast-forward git push to `main` → Vercel production build._

## 2026-07-25
- _production deployed, mobile visual smoke passed:_ PR #7 merged D-501 as `8adfd1b`; Vercel completed Production deployment successfully. D-501 fixes the 390px Classic Loafer PDP overflow caused by content-box padding outside the fixed CTA's 40/60 split. Both controls use `boxSizing: 'border-box'` plus `minWidth: 0`; `test:storefront-trust`, typecheck, lint, full `npm run validate`, and `npm run build` pass. Public desktop smoke confirmed homepage/PDP rendering, Shopier/WhatsApp CTAs, and no browser errors. A user-supplied Chrome DevTools responsive `390 x 844` production screenshot visually confirms the fixed controls fit with no visible horizontal overflow. No Payload write, provider call, Shopier call, Telegram action, n8n action, external dispatch, ad spend, SupplierScout activation, or retired-channel activation occurred.

- _production deployed, integrations unverified:_ PR #6 merged the validated D-380-D-500 master-build stack into `main`. GitHub reported it cleanly mergeable with no review comments. Vercel Production deployment `dpl_5L6CXiNjKBiqS8sp7DAuRabcG1cj` completed Ready at `https://www.uygunayakkabi.com`; read-only browser smoke confirmed the homepage and live Classic Loafer PDP render with Shopier and WhatsApp CTAs and no console errors. Live Telegram command, Shopier webhook/API delivery, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, and optional OpenClaw sync remain unverified and were not performed.

- _local only, not deployed:_ D-500 unifies Facebook direct dispatch and provider-health on deployment env `INSTAGRAM_PAGE_ID`, rather than the removed Payload `facebookPageId` column. `.env.example` now removes retired Dolap/Threads fallback variables and lists the current X OAuth 1.0a contract. `test:provider-health`, `test:meta-provider-credentials`, `test:channel-dispatch`, source/release/PR/Obsidian governance, full `npm run validate`, `npm run build`, and `git diff --check` pass. The approved read-only provider-health smoke reports Website `ready/native`, Facebook `ready/direct`, Instagram disabled, and X/Shopier missing their current requirements. No provider, n8n, Payload, queue, Telegram, commit, branch, PR, or deployment action occurred.

- _local only, not deployed:_ D-495 blocks Instagram/Facebook before direct Meta or optional n8n fallback when no public HTTPS gallery image exists. Mocked tests prove both channels make zero fetch calls while retaining fallback configuration visibility in the failure result; full `npm run validate` and `npm run build` pass. No Meta, n8n, Payload, queue, Telegram, commit, branch, PR, or deployment action occurred.

- _local only, not deployed:_ D-494 makes Instagram/Facebook direct selection scan the whole gallery for public HTTPS media instead of requiring image zero. Mocked adapter tests show both direct paths choose a later valid image; full `npm run validate` and `npm run build` pass. No Meta, n8n, Payload, queue, Telegram, commit, branch, PR, or deployment action occurred. Production media reachability and real delivery remain unproven.

- _local only, not deployed:_ D-493 aligns X direct dispatch with its provider-health contract. Direct X requires `X_API_KEY`, `X_API_SECRET`, `X_ACCESS_TOKEN`, and `X_ACCESS_TOKEN_SECRET`; partial configuration uses optional `N8N_CHANNEL_X_WEBHOOK` only when configured, otherwise it records missing configuration without a direct X API call. Focused dispatch/provider-health/redispatch/dispatch-status tests, full `npm run validate`, and `npm run build` pass. No provider/webhook call, Payload write, queue write, Telegram action, commit, branch, PR, or deployment occurred.

- _configured-database schema action, code not deployed:_ After approved D-481 preflight and dry-run SQL review, `npm run db:shopier-order-id-unique:apply -- --apply --confirm-apply-d481-shopier-order-id-unique` completed successfully. The reviewed concurrent partial unique-index SQL fingerprint was `c79810ec7a084bfc`. Post-apply read-only verification confirms nullable `orders.shopier_order_id`, zero duplicate non-empty IDs, and present `orders_shopier_order_id_unique_idx`; PostgreSQL canonicalizes the blank-ID predicate as `btrim((shopier_order_id)::text)`. The checker was corrected to recognize that equivalent form, and `npm run test:shopier-webhook-local` passes. No application deploy, live Shopier webhook/API delivery, Payload business-data write, provider call, dispatch, queue write, Telegram action, commit, branch, PR, or deployment occurred.

- _configured-database metadata only, no deploy:_ Approved D-481/D-489/D-490/D-491 schema preflights ran with read-only catalog metadata. D-489 `public.wizard_sessions` is complete, D-490 contains every required CustomerInquiries status enum value, and D-491 has nullable integer `orders.related_inquiry_id` plus its `customer_inquiries.id` foreign key. D-481 found `orders.shopier_order_id` present and nullable with zero duplicate non-empty values, but `orders_shopier_order_id_unique_idx` is absent. Its no-connection dry run printed SQL fingerprint `c79810ec7a084bfc` for the reviewed concurrent partial unique-index statement. No DDL, Payload business-data write, Shopier call, provider call, dispatch, queue write, Telegram action, commit, branch, PR, or deployment occurred. The D-481 partial unique-index apply remains separately approval-gated.

## 2026-07-24
- _local only, not deployed:_ D-482 Shopier order transaction boundary prepared locally. A verified `order.created` now creates its local Order and applies product/variant stock plus InventoryLog writes through one Payload transaction request; transaction setup fails closed, and a verified processing failure returns `500` for Shopier retry. The generic Orders alert hook skips Shopier while the webhook alert is post-commit. Focused transaction, stock, idempotency, webhook-local checks, full `npm run validate`, and `npm run build` pass. No configured-database metadata read, DDL, live Shopier webhook/API, Payload write, external dispatch, commit, branch, PR, or deployment was performed. D-481's reviewed unique index is now applied and post-apply verified.

- _local only, not deployed:_ D-481 Shopier order-ID duplicate-safety guard prepared and validated locally. `shopierOrderId` now has a unique contract, and a PostgreSQL duplicate-key from order creation returns before stock decrement or Telegram notification. A reviewed partial `CREATE UNIQUE INDEX CONCURRENTLY` plan is dry-run-first; its dry run did not connect to PostgreSQL. Focused helper/governance checks, `npm run validate`, and `npm run build` pass. No configured-database metadata preflight, index DDL, live Shopier webhook/API, Payload write, external dispatch, commit, branch, PR, or deployment was performed.

- _local only, not deployed:_ D-463 Mentix skill runtime-truth reconciliation prepared and validated locally. Repo-side skills, activation configuration, dashboard, optional sync checklist, and installation matrix now consistently name Hermes/Mentix as current and OpenClaw as historical/optional pending explicit reactivation plus VPS verification. Product-flow diagnostics stay read-only; upload/research stay operator-reviewed drafts; backend advice stays non-deploying; durable memory stays PII-light project control knowledge. Mentix/VPS, source/release/PR, retired-channel, n8n, ops, typecheck, lint, `git diff --check`, and full `npm run validate` passed with 0 lint errors / 71 warnings. No VPS sync/restart, live Telegram/OpenClaw prompt, n8n workflow, provider call, Shopier call, dispatch, product/lead/order write, ad spend, commit, branch, PR, deployment, SupplierScout activation, or retired-channel activation has been performed.

- _configured-database schema action, code not deployed:_ D-462 BlogPosts featured-image schema drift repair was applied after the approved 2026-07-24 metadata preflight confirmed that `blog_posts` and `media` exist while the declared relationship column and foreign key were missing. The additive migration created `featured_image_id`, the exact `ON DELETE SET NULL` media foreign key, and its supporting index; the post-apply check passes and the next build completes without the sitemap fallback. `npm run db:blog-featured-image:apply` remains dry-run by default and requires explicit operator approval plus `--apply --confirm-apply-d462-blog-featured-image-schema` in another environment. No commit, branch, push, PR, deploy, live Telegram command, webhook call, n8n workflow change, OpenClaw sync, product/lead/order write, Shopier call, provider call, ad spend, SupplierScout activation, or retired-channel activation has been performed.

- _local only, not deployed:_ D-461 control-truth Memory Lock reconciliation prepared and validated locally. Both session-start Memory Lock files now state Payload/Next execution, Hermes-current control, OpenClaw/n8n optionality, direct Payload/Next default, and SupplierScout dormancy; governance rejects the old live Telegram-to-OpenClaw-to-n8n pipeline. `npm run test:retired-channels`, `npm run test:n8n-optional`, `npm run test:mentix-skills`, source/release/PR/ops governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, VPS action, webhook call, n8n workflow change, OpenClaw sync, product/lead/order write, Shopier call, provider call, ad spend, SupplierScout activation, or retired-channel activation has been performed.
- _local only, not deployed:_ D-460 product-flow dispatch recovery paths prepared and validated locally. `/productflow`, `/flow`, and `smoke:product-flow:read` now surface a deterministic next operator action beside every non-published active-channel dispatch state/reason. Queued Shopier stays on `/shopier dashboard`; ready-but-unrecorded Shopier stays on shared guarded `/shopier publish <ref>`; failed redispatchable rows only suggest retry after the recorded cause is fixed. `npm run test:product-flow-snapshot`, `npm run test:runtime-smokes`, source/release/PR/ops governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, redispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.

## 2026-07-18
- _local only, not deployed:_ D-459 product-flow dispatch summary prepared and validated locally. `/productflow`, `/flow`, and `smoke:product-flow:read` now surface active-channel published/queued/failed/blocked/not-configured/unrecorded dispatch counts before the full dispatch rows. `npm run test:product-flow-snapshot`, `npm run test:runtime-smokes`, source/release/PR/ops governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, redispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-458 product-flow checklist summary prepared and validated locally. `/productflow`, `/flow`, and `smoke:product-flow:read` now surface done/next/blocked/needs-work checklist counts before the full staged checklist. `npm run test:product-flow-snapshot`, runtime/source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, Pixel/CAPI action, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-457 loading-plan focus details prepared and validated locally. `/loadplan`, `/loadingplan`, and `smoke:load-plan:read` now surface reason details beside each focus-queue command for the current catalog bottleneck. `npm run test:loading-plan`, runtime/source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, Pixel/CAPI action, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-456 loading-plan focus queue prepared and validated locally. `/loadplan`, `/loadingplan`, and `smoke:load-plan:read` now surface focus refs and matching safe read commands for the current catalog bottleneck. `npm run test:loading-plan`, runtime/source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, Pixel/CAPI action, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-455 loading-plan batch focus prepared and validated locally. `/loadplan`, `/loadingplan`, and `smoke:load-plan:read` now surface focus kind, operator label, reason, and next safe read before product rows so daily catalog loading starts from the safest bottleneck. `npm run test:loading-plan`, runtime/source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, Pixel/CAPI action, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-454 loading-plan batch summary prepared and validated locally. `/loadplan`, `/loadingplan`, and `smoke:load-plan:read` now surface worklist candidate count, priority counts, blocker counts, first suggested command, first `/productflow` handoff, and first repo-side product-flow smoke command before product rows. `npm run test:loading-plan`, runtime/source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, Pixel/CAPI action, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.

## 2026-07-16
- _local only, not deployed:_ D-453 source-pack latest-boundary guardrail prepared and validated locally. `test:source-pack` now requires next-sprint source-pack notes to carry D-453 as the latest local boundary, retain `Operator Live Smoke Plan (D-389/D-452)` as the actual smoke-plan title boundary, describe the D-380-D-406 plus D-422-D-453 release/PR stack, and reject stale current-D-449 or D-422-D-451 stack wording. `npm run test:source-pack`, release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, Pixel/CAPI action, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-452 ad-readiness storefront trust hint prepared and validated locally. `/adready` now shows `npm run test:storefront-trust` for review/ready products before `/adpack` and `/adreport`, keeping D-451 PDP conversion/trust checks visible before manual paid-traffic prep; blocked products still point to `/productflow` and `/imageplan` diagnostics first. `/smokeplan` now renders `Operator Live Smoke Plan (D-389/D-452)`. `npm run test:ad-readiness`, `npm run test:operator-smoke-plan`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, Pixel/CAPI action, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-451 PDP conversion trust guardrail prepared and validated locally. `test:storefront-trust` now also checks `src/app/(app)/products/[slug]/page.tsx` so public product pages keep draft hiding, mounted gallery, variant-backed size/stock clarity, lead form product context, WhatsApp CTA, Shopier CTA, FAQ fallback, and safe similar-products gating before paid-traffic readiness. `npm run test:storefront-trust`, `npm run test:source-pack`, `npm run test:local-release-candidate`, `npm run test:local-pr-review`, `npm run typecheck`, `npm run lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No build, DB read, network call, commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-450 retired-channel memory-lock guardrail prepared and validated locally. `test:retired-channels` now checks `project-control/MEMORY_LOCK.md` and `project-control/exports/MEMORY_LOCK.md`, requiring active channels to remain Website/Instagram/Facebook/X/Shopier and Dolap/Threads to be retired rather than scaffolded/planned/active. `test:retired-channels`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No runtime behavior change, commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-449 smoke-plan latest-boundary label prepared and validated locally. `/smokeplan` now renders `Operator Live Smoke Plan (D-389/D-449)` so the operator-facing checklist reflects the D-448 ad-readiness next-read guidance and latest local handoff boundary. `test:operator-smoke-plan`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No smoke-order change, commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-448 ad-readiness next-action hints prepared and validated locally. `/adready` now shows safe next reads for readiness state: `/productflow`, `/imageplan`, `/adpack <ref> manual_ads`, and `/adreport week` as applicable. `test:ad-readiness`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product/lead/order write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-447 funnel snapshot next-action hints prepared and validated locally. `/funnel` now shows safe next reads for lead-source/order/UTM evidence: `/leadplan`, `/orders`, and `/adreport week`. `test:funnel-desk`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, lead/order/product write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-446 business snapshot next-action hints prepared and validated locally. `/business` now shows safe next reads for urgency counts: `/leadplan`, `/orderreminders`, `/orders`, and `/inbox stock`. `test:business-desk`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, lead/order/product write, stock change, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-445 order desk operator links prepared and validated locally. `/inbox orders`, `/orders`, `/orders today`, `/orderreminders`, `/order <id>`, and new-order alerts now show Payload order-admin links, related product-admin and lead-admin links when present, and public PDP links only for related products with public status. `test:order-desk`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, order status write, stock restore, customer message, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-444 lead desk operator links prepared and validated locally. `/inbox leads`, `/leadreminders`, `/leads`, `/lead <id>`, and new-lead alerts now show Payload lead-admin links, related product-admin links when present, and public PDP links only for related products with public status. `test:lead-desk`, `test:lead-followup-plan`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, lead write, customer message, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-443 operator inbox product links prepared and validated locally. `/inbox pending`, `/inbox publish`, `/inbox stock`, `/inbox failed`, and `/inbox today` product rows now show Payload admin links plus public PDP links only for products with public status. `test:operator-inbox`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product write, activation, queue job, publish, dispatch, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-442 lead follow-up operator links prepared and validated locally. `/leadplan`, `/followupplan`, and `smoke:lead-followup:read` now show lead-admin links, related product-admin links when present, and public PDP links only for related products with public status. `test:lead-followup-plan`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, lead write, customer message, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, external dispatch, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-441 Shopier preview credential holds prepared and validated locally. `/shopier publish-ready`, `/shopier retry-errors`, and `smoke:shopier:read` preview rows now show whether `SHOPIER_PAT` is configured before confirm commands. Missing credentials keep preview available but point operators to configure `SHOPIER_PAT`; configured credentials still tell operators to verify webhook/account/quota outside chat. `test:shopier-publish-control`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, secret print, direct Shopier call, provider call, ad-platform API call, campaign/post/pixel creation, external dispatch, queue write outside explicit confirmed queue paths, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-440 Shopier preview/dashboard operator links prepared and validated locally. `/shopier dashboard`, `/shopier publish-ready`, `/shopier retry-errors`, and `smoke:shopier:read` preview/review rows now show Payload admin links for products with ids and public PDP links only for products with slugs plus public status; confirmed queue/retry output stays free of preview-only links. `test:shopier-publish-control`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, direct Shopier call, provider call, ad-platform API call, campaign/post/pixel creation, external dispatch, queue write outside explicit confirmed queue paths, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-439 loading-plan worklist operator links prepared and validated locally. `/loadplan`, `/loadingplan`, and `smoke:load-plan:read` first product worklist rows now show Payload admin links for products with ids and public PDP links only for products with slugs plus public status. `test:loading-plan`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product write, lead/order mutation, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-438 Product Flow Snapshot operator links prepared and validated locally. `/productflow`, `/flow`, and `smoke:product-flow:read` now show Payload admin links for products with ids and public PDP links only for products with slugs plus public status. `test:product-flow-snapshot`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product write, lead/order mutation, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-437 operator smoke-plan Telegram access preflight prepared and validated locally. `/smokeplan` now runs `npm run test:telegram-access` after `smoke:load-plan:read` and before the first Telegram `/loadplan` read, keeping private Telegram DM allowlist behavior visible before live Telegram operator reads. `test:operator-smoke-plan`, `test:telegram-access`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product write, lead/order mutation, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-436 operator smoke-plan sitemap preflight prepared and validated locally. `/smokeplan` now runs `npm run test:sitemap-entries` after attribution and before manual ad-readiness checks, keeping static route, website-visible product, and blog sitemap/degrade-safe checks in the paid-traffic preflight sequence. `test:operator-smoke-plan`, `test:sitemap-entries`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product write, lead/order mutation, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-435 operator smoke-plan attribution preflight prepared locally; validated locally. `/smokeplan` now runs `npm run test:attribution` after inquiry guard and before manual ad-readiness checks, keeping first-touch UTM/referrer capture, storefront navigation preservation, and lead-submit attribution merge checks in the paid-traffic preflight sequence. `test:operator-smoke-plan`, `test:attribution`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product write, lead/order mutation, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-434 operator smoke-plan inquiry guard preflight prepared locally; validated locally. `/smokeplan` now runs `npm run test:inquiry-guard` after storefront trust and before manual ad-readiness checks, keeping honeypot, rate-limit, duplicate-collapse, and safe lead-form fallback checks in the paid-traffic preflight sequence. `test:operator-smoke-plan`, `test:inquiry-guard`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product write, lead/order mutation, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-433 operator smoke-plan storefront trust preflight prepared and validated locally. `/smokeplan` now runs `npm run test:storefront-trust` after lead visibility and before manual ad-readiness checks, keeping the fake-review/placeholder-testimonial guard in the paid-traffic preflight sequence. `test:operator-smoke-plan`, `test:storefront-trust`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product write, lead/order mutation, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.

## 2026-07-12
- _local only, not deployed:_ D-432 operator smoke-plan manual ad preflight alignment prepared and validated locally. `/smokeplan` now runs read-only `smoke:ad-readiness:read`, Telegram `/adready`, `smoke:ad-performance:read`, and Telegram `/adreport week` after lead visibility and before Shopier queue preflights. `test:operator-smoke-plan`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, product write, lead/order mutation, provider call, Shopier call, ad-platform API call, campaign/post/pixel creation, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-431 operator smoke-plan Shopier credential hold prepared and validated locally. `/smokeplan` now includes a dedicated hold after Shopier row product-flow handoffs and before final queue approval, telling operators to verify `SHOPIER_PAT`, Shopier webhook readiness, account permission, and quota/readiness outside chat without pasting secrets before any Shopier confirm action. `test:operator-smoke-plan`, source/release/PR/runtime governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, secret read, live Shopier call, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-430 operator smoke-plan Shopier handoff alignment prepared and validated locally. `/smokeplan` now includes a dedicated hold after Shopier dashboard/publish-ready/error/retry previews telling operators to use row-provided `/productflow <ref>` and exact repo `npm run smoke:product-flow:read -- --product=<ref> --confirm-read-only` handoffs before any Shopier confirm action. `test:operator-smoke-plan`, source/release/PR/runtime governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, live Shopier call, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-429 Shopier preview product-flow handoff prepared and validated locally. `/shopier publish-ready` and `/shopier retry-errors` preview rows now show Telegram `/productflow <ref>` and exact repo `npm run smoke:product-flow:read -- --product=<ref> --confirm-read-only` preflight commands before confirm. Telegram previews can render without `SHOPIER_PAT`; confirmed queue/retry actions remain blocked without `SHOPIER_PAT`. `test:shopier-publish-control`, `test:shopier-commands`, source/release/PR/runtime governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, live Shopier call, provider call, external dispatch, queue write outside explicit local tests, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-428 Shopier dashboard product-flow handoff prepared and validated locally. `/shopier dashboard` and `smoke:shopier:read` batch review rows now show each existing next action plus Telegram `/productflow <ref>` and exact repo `npm run smoke:product-flow:read -- --product=<ref> --confirm-read-only` preflight commands before Shopier queue decisions. `test:shopier-publish-control`, `test:runtime-smokes`, source/release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, live Shopier call, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-427 load-plan runtime product-flow handoff prepared and validated locally. `/loadplan`, `/loadingplan`, and `smoke:load-plan:read` first product worklist rows now show both Telegram `/productflow <ref>` and exact repo `npm run smoke:product-flow:read -- --product=<ref> --confirm-read-only` preflight commands, so terminal and Telegram operators use the same selected product. `test:loading-plan`, `test:runtime-smokes`, `test:operator-smoke-plan`, release/PR/source-pack/ops governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, live Shopier call, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-426 operator smoke-plan load-plan handoff alignment prepared and validated locally. `/smokeplan` now runs the worklist-selected `smoke:product-flow:read` and Telegram `/productflow <id-or-sn>` checks immediately after repo/Telegram `/loadplan`, before provider diagnostics, so the D-425 flow handoff is the first live-smoke product preflight. `test:operator-smoke-plan`, release/PR/source-pack/ops governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, live Shopier call, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-425 load-plan product-flow handoff prepared and validated locally. `/loadplan`, `/loadingplan`, and `smoke:load-plan:read` first product worklist rows now show `/productflow <ref>` beside the suggested action command, so catalog loading can pass through the D-424 primary operator step before manual follow-up. `test:loading-plan`, release/PR/source-pack/ops governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, live Shopier call, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-424 product-flow primary operator step prepared and validated locally. `/productflow`, `/flow`, and `smoke:product-flow:read` now show one `primaryOperatorStep` derived from the ordered checklist before the full checklist. `test:product-flow-snapshot`, runtime-smoke governance, source-pack, Mentix skill, ops-runbook, release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, live Shopier call, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-423 product-flow checklist dependency ordering prepared and validated locally. `/productflow`, `/flow`, and `smoke:product-flow:read` now keep operator checklist commands in workflow order: confirm before content, and content trigger/retry before audit. `test:product-flow-snapshot`, source-pack, Mentix skill, ops-runbook, runtime-smoke, release/PR governance, `typecheck`, `lint` with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, live Shopier call, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-422 product-flow operator checklist prepared and validated locally. `/productflow`, `/flow`, and `smoke:product-flow:read` now include a read-only checklist for Photos/Image QC, confirmation, content, audit, price/size/stock, channel targets, operator approval, and Shopier queue state when relevant. `test:product-flow-snapshot`, source-pack, Mentix skill, ops-runbook, runtime-smoke, release/PR governance, `npm run validate` with lint at 0 errors / 71 warnings, and `git diff --check` passed locally. No commit, branch, push, PR, deploy, live Telegram command, live Shopier call, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.

## 2026-07-11
- _local only, not deployed:_ Hermes current control layer reconciliation prepared and validated. Docs/governance now align so Hermes is current, Mentix/Uygunops is the Telegram-facing operator identity/interface, Payload/Next remains source of truth/execution, and OpenClaw is historical/optional unless explicitly reactivated. `test:openclaw-vps-verification` remains standalone for optional OpenClaw reactivation review, not normal `test:safe` while Hermes is current. Source-pack, Mentix skill, optional OpenClaw verification, release/PR, ops governance, typecheck, lint with 0 errors / 71 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, live Shopier call, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or OpenClaw sync has been performed.

## 2026-07-06
- _local only, not deployed:_ D-406 Shopier runtime-smoke batch review alignment prepared and validated locally. `smoke:shopier:read` now builds `reviewRows` with `buildShopierDashboardReviewRows()` and passes them into the same `formatShopierOperatorDashboard()` used by Telegram `/shopier dashboard`, so the repo-side smoke mirrors ready/blocked/queued/synced batch review rows. `test:runtime-smokes`, source-pack/release/PR/ops governance, `typecheck`, `lint` with 0 errors / 70 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, live Shopier call, provider call, external dispatch, queue write, ad spend, SupplierScout activation, or retired-channel activation has been performed.
- _local only, not deployed:_ D-405 image-plan runtime smoke prepared and validated locally. `scripts/image-plan-runtime-smoke.ts` and `smoke:image-plan:read` mirror Telegram `/imageplan` against one real Payload product plus recent `image-generation-jobs` only after `--confirm-read-only`, force `PAYLOAD_DB_PUSH=false`, and refuse mutation/queue/publish/dispatch/provider/Shopier/SupplierScout/retired-channel/spend paths. `/smokeplan` now places this repo smoke before Telegram `/imageplan`. No-connect help, `test:runtime-smokes`, `test:operator-smoke-plan`, release/PR/source-pack/ops governance, `typecheck`, `lint` with 0 errors / 70 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, image-generation queue, provider call, Shopier call, external dispatch, ad spend, SupplierScout activation, or retired-channel activation has been performed.
- _local only, not deployed:_ D-404 image regeneration plan prepared and validated locally. `src/lib/imageRegenerationPlan.ts`, Telegram `/imageplan <sn-or-id>`, and Telegram `/regenplan <sn-or-id>` now read product Image QC plus recent image-generation job state and suggest safe manual next commands. `/smokeplan` includes `/imageplan <id-or-sn>` after `/productflow`. `test:image-regeneration-plan`, `test:operator-smoke-plan`, release/PR/source-pack/ops governance, `typecheck`, `lint` with 0 errors / 70 warnings, `git diff --check`, and full `npm run validate` passed locally. No commit, branch, push, PR, deploy, live Telegram command, image-generation queue, provider call, Shopier call, external dispatch, ad spend, SupplierScout activation, or retired-channel activation has been performed.

## 2026-07-05
- _local only, not deployed:_ D-403 provider reality audit prepared and validated. `project-control/PROVIDER_REALITY_AUDIT.md` now records that local env readiness is not production provider readiness for channel providers, Product Intelligence/GEO providers, reverse-search selection, and n8n fallback webhooks. `test:provider-reality` was added to `test:safe`. `test:provider-reality`, release/PR governance, source-pack governance, ops-runbook governance, runtime-smoke governance, retired-channel governance, Product Intelligence provider-health, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. No commit, branch, push, PR, deploy, env load, secret print, provider call, credit spend, live Telegram command, live Shopier webhook smoke, external dispatch, queue write, ad spend, SupplierScout activation, or retired-channel activation has been performed.
- _local only, not deployed:_ D-402 historical soak-script quarantine prepared and validated. `project-control/HISTORICAL_SOAK_SCRIPTS.md` now documents old `scripts/d*-soak*.ts` files as historical live-data soak harnesses, not validation and not read-only runtime smokes. `test:soak-scripts` was added to `test:safe`. `test:soak-scripts`, release/PR governance, source-pack governance, ops-runbook governance, runtime-smoke governance, retired-channel governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. No commit, branch, push, PR, deploy, live soak run, live Telegram command, live Shopier webhook smoke, provider call, external dispatch, queue write, ad spend, SupplierScout activation, or retired-channel activation has been performed.
- _local only, not deployed:_ D-401 OpenClaw VPS verification guardrail prepared and validated. `mentix-skills/OPENCLAW_VPS_VERIFICATION.md` now defines read-only evidence collection before any OpenClaw skill copy, restart, or live prompt; `mentix-skills/INSTALLATION_MATRIX.md` now keeps VPS state at `VERIFY ON VPS` unless current evidence exists; `test:openclaw-vps-verification` is now standalone optional OpenClaw reactivation coverage while Hermes is current. `test:openclaw-vps-verification`, `test:mentix-skills`, release/PR governance, source-pack governance, ops-runbook governance, retired-channel governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. No commit, branch, push, PR, deploy, VPS command, VPS sync, OpenClaw restart, live Telegram command, live Shopier webhook smoke, provider call, external dispatch, queue write, ad spend, SupplierScout activation, or retired-channel activation has been performed.
- _local only, not deployed:_ D-400 Shopier dashboard batch review sample prepared and validated. `/shopier dashboard` now includes read-only ready/blocked/queued/synced sample rows using the shared Shopier/Web evaluator, with product ref, detail/blocker, and suggested manual command. `test:shopier-publish-control`, release/PR governance, source-pack governance, ops-runbook governance, retired-channel governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. No commit, branch, push, PR, deploy, live Telegram command, live Shopier webhook smoke, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-399 load-plan runtime smoke output alignment prepared and validated. `smoke:load-plan:read` now prints the first product worklist from `plan.worklist`, and `test:runtime-smokes` guards that output surface. `test:runtime-smokes`, `test:loading-plan`, release/PR governance, source-pack governance, ops-runbook governance, retired-channel governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. No commit, branch, push, PR, deploy, live Telegram command, live Shopier webhook smoke, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-399 loading-plan first product worklist prepared and validated. `/loadplan` and `smoke:load-plan:read` now include a read-only first product worklist with product ref, title, priority, reasons, and suggested manual command. `test:loading-plan`, `test:local-release-candidate`, `test:local-pr-review`, source-pack governance, ops-runbook governance, retired-channel governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. No commit, branch, push, PR, deploy, live Telegram command, live Shopier webhook smoke, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-398 local PR review package prepared and validated. `project-control/LOCAL_PR_REVIEW_PACKAGE.md` now prepares the D-380-D-404 local stack for human review with proposed PR title, scope summary, reviewer focus, validation commands, 19-file source-pack count, and not-run/not-done guardrails. `test:local-pr-review` was added to `test:safe`. `test:local-pr-review`, `test:local-release-candidate`, source-pack governance, ops-runbook governance, retired-channel governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. No commit, branch, push, PR, deploy, live Telegram command, live Shopier webhook smoke, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-397 local release candidate boundary prepared and validated. `project-control/LOCAL_RELEASE_CANDIDATE.md` now records the D-380-D-404 local stack as not committed and not deployed, with current architecture invariants, validation boundary, 19-file source-pack count, and operator approval requirements before commit/PR/deploy/live smoke/Shopier/provider/VPS/ad work. `test:local-release-candidate` was added to `test:safe`. `test:local-release-candidate`, source-pack governance, ops-runbook governance, retired-channel governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. No commit, push, PR, deploy, live Telegram command, live Shopier webhook smoke, provider call, external dispatch, queue write, ad spend, SupplierScout activation, retired-channel activation, or optional OpenClaw sync has been performed.
- _local only, not deployed:_ D-396 smokeplan lead-followup alignment prepared and validated. `/smokeplan` now includes `smoke:lead-followup:read` after business/funnel visibility and before Telegram `/leadplan`, then continues to Shopier preflights. `test:operator-smoke-plan`, source-pack governance, retired-channel governance, runtime-smoke governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. No production deploy, live Telegram run, lead write, customer message, queue write, Shopier call, provider call, ad spend, SupplierScout activation, or retired-channel activation has been performed.

## 2026-07-04
- _local only, not deployed:_ D-395 lead-followup runtime smoke prepared and validated. `smoke:lead-followup:read` mirrors Telegram `/leadplan` and `/followupplan` against real Payload leads only after `--confirm-read-only`, prints PII-light lead-priority output, and refuses lead writes, customer messages, provider calls, Shopier calls, ad spend, SupplierScout activation, retired-channel activation, and schema push. No-connect help, `test:runtime-smokes`, source-pack governance, retired-channel governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings.
- _local only, not deployed:_ D-394 lead follow-up plan prepared and validated. `/leadplan` and `/followupplan` read open Payload leads, prioritize stale/new follow-up work, and suggest existing manual lead commands only. `test:lead-followup-plan`, source-pack governance, retired-channel governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. No production deploy, live Telegram smoke, provider call, Shopier call, ad action, SupplierScout activation, or retired-channel activation has been performed.
- _local only, not deployed:_ D-393 operator smoke-plan Shopier webhook preflight prepared and validated. Added `npm run test:shopier-webhook-local`, inserted it into `/smokeplan` before `smoke:shopier:read`, and updated ops-runbook governance so local Shopier stock/refund webhook assertions run before any read-only Shopier runtime smoke or operator-approved live webhook smoke. `test:shopier-webhook-local`, `test:operator-smoke-plan`, `test:ops-runbook`, source-pack governance, retired-channel governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings.
- _local only, not deployed:_ D-392 Shopier refund-request idempotency prepared and validated. `refund.requested` now records an idempotent local request marker before stock restore and the webhook route restores stock only when that marker is first recorded, so duplicate Shopier delivery cannot restore inventory twice. Legacy `Iade talebi:` notes are treated as already-recorded refund requests. `test:shopier-refund-lifecycle`, `test:shopier-order-stock`, `test:order-desk`, source-pack governance, ops-runbook governance, retired-channel governance, `typecheck`, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. Awaiting commit/push/deploy approval and later operator-approved live webhook smoke.
- _local only, not deployed:_ D-391 Shopier refund update traceability prepared and validated. `refund.updated` now appends an idempotent refund-status note to the matching Payload order and emits `order.refund_updated` when possible, without changing order status, restoring stock a second time, calling Shopier, dispatching channels, queueing jobs, activating SupplierScout, reviving retired channels, or spending on ads. `test:shopier-refund-lifecycle`, `test:shopier-order-stock`, `test:order-desk`, source-pack governance, ops-runbook governance, retired-channel governance, `git diff --check`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. Awaiting commit/push/deploy approval and later operator-approved live webhook smoke.

## 2026-07-03
- _local only, not deployed:_ D-390 Mentix/OpenClaw live-smoke alignment prepared and validated. Repo-side `mentix-intake`, `product-flow-debugger`, OpenClaw sync checklist, installation matrix, and skill dashboard now point live-smoke planning to `/smokeplan` first and keep n8n optional. `test:mentix-skills`, `test:operator-smoke-plan`, and full `npm run validate` passed locally with lint at 0 errors / 70 warnings. Awaiting commit/push/deploy/VPS sync approval.
- _local only, not deployed:_ D-389 operator live smoke plan prepared and validated. `/smokeplan` returns the safe read-only sequence for load-plan, provider diagnostics, product-flow, business/funnel, and Shopier dashboard/error/retry checks, then stops before queueing, publishing, redispatch, provider spend, Shopier API actions, or ads. `test:operator-smoke-plan`, `test:retired-channels`, `test:source-pack`, `test:ops-runbook`, `typecheck`, `lint`, `git diff --check`, and full `npm run validate` passed locally. Awaiting commit/push/deploy approval.
- _local only, not deployed:_ D-388 load-plan runtime smoke prepared and validated. `smoke:load-plan:read` mirrors Telegram `/loadplan` through `buildProductLoadingPlan()` against real Payload products only after `--confirm-read-only`, forces `PAYLOAD_DB_PUSH=false`, and refuses mutation/publish/queue/provider/Shopier/SupplierScout/ad-spend flags. No-connect help, `test:runtime-smokes`, `test:ops-runbook`, `test:source-pack`, `test:retired-channels`, `typecheck`, `lint`, `git diff --check`, and full `npm run validate` passed locally. No live read-only run has been performed yet; awaiting commit/push/deploy approval.
- _local only, not deployed:_ D-387 product loading plan prepared and validated. `/loadplan [limit]` and `/loadingplan [limit]` compose Catalog QA and Category Fill into read-only catalog scale-up actions, with guardrails against product writes, publishing, Shopier queueing, provider calls, SupplierScout activation, Dolap/Threads revival, and ad spend. `test:loading-plan`, `test:retired-channels`, `test:source-pack`, `typecheck`, `lint`, `git diff --check`, and full `npm run validate` passed locally. Awaiting commit/push/deploy approval.
- _local only, not deployed:_ D-386 Shopier command governance prepared and validated. Telegram `/shopier publish` and `/shopier republish` no longer carry unreachable direct queue/update branches; `test:shopier-commands` checks they resolve products and use the shared `queueShopierSync()` gate instead of route-level `shopier-sync` writes. `test:shopier-commands`, `test:shopier-publish-control`, `test:source-pack`, `typecheck`, `lint`, `git diff --check`, and full `npm run validate` passed locally. Awaiting commit/push/deploy approval.
- _local only, not deployed:_ D-385 runtime-smoke governance prepared and validated. `test:runtime-smokes` checks that read-only smoke commands have package scripts, backing scripts, explicit read-only confirmations, mutation refusal, no-write wording, Payload schema-push guards where relevant, and synchronized runtime smoke docs. `test:runtime-smokes`, `test:source-pack`, `test:ops-runbook`, `typecheck`, `lint`, `git diff --check`, and full `npm run validate` passed locally. Awaiting commit/push/deploy approval.
- _local only, not deployed:_ D-384 ad-performance runtime smoke prepared and validated. `smoke:ad-performance:read` mirrors `/adreport` against real Payload leads/orders only after `--confirm-read-only`, forces `PAYLOAD_DB_PUSH=false`, and refuses mutation/publish/queue/provider/Shopier/spend flags. No-connect help, `test:source-pack`, `test:ops-runbook`, `typecheck`, `lint`, `git diff --check`, and full `npm run validate` passed locally. No live read-only smoke has been run yet; awaiting commit/push/deploy approval.
- _local only, not deployed:_ D-383 manual ad performance report prepared and validated. `/adreport [today|week|month]` is read-only and summarizes UTM-tagged Payload leads, related orders, revenue, stale open leads, unattributed leads, and direct/unattributed orders without Pixel/CAPI/Ads API, campaign creation, external posting, provider calls, Shopier calls, external API calls, or ad spend. `test:ad-performance`, `typecheck`, `lint`, `git diff --check`, and full `npm run validate` passed locally. Awaiting commit/push/deploy approval.
- _local only, not deployed:_ D-382 story dispatch lint cleanup prepared and validated. Removed stale unused imports from `src/lib/storyDispatch.ts`; `npm run lint` now reports 0 errors and 70 warnings, down from 74 warnings. `test:story-dispatch`, `typecheck`, `git diff --check`, and full `npm run validate` passed locally. Awaiting commit/push/deploy approval.
- _local only, not deployed:_ D-381 story dispatch brand-safety hardening prepared and validated. `dispatchStory()` now blocks protected-brand products before StoryJob creation and records a failed story status with a `brand_safety_block` reason instead of queueing a future story/social job. `test:story-dispatch`, `typecheck`, `lint`, `git diff --check`, and full `npm run validate` passed locally. Awaiting commit/push/deploy approval.
- _local only, not deployed:_ D-380 manual ad launch-pack support prepared and validated. `/adpack <sn-or-id> [campaign]` is read-only and builds operator-review copy drafts plus Meta paid-social UTM links without creating campaigns, posts, pixels, provider calls, Shopier calls, or ad spend. `test:ad-launch-pack`, `typecheck`, `lint`, `git diff --check`, and full `npm run validate` passed locally. Awaiting commit/push/deploy approval.
- _local only, not deployed:_ Phase 7 Shopier fulfilled lifecycle unification prepared and validated. `order.fulfilled` now routes through `applyOrderStatus(..., 'ship', 'shopier_webhook')` instead of direct status overwrite, with audit-source coverage in `test:order-desk`. `test:order-desk`, `typecheck`, `lint`, `git diff --check`, and full `npm run validate` passed locally. Awaiting commit/push/deploy approval and later operator-approved live webhook smoke after Shopier credentials/webhooks are configured.
- _local only, not deployed:_ Phase 7 Shopier order/refund stock reconciliation helper prepared. `src/lib/shopierOrderStock.ts` is wired into the Shopier webhook route; `test:shopier-order-stock`, `typecheck`, `lint`, `git diff --check`, and full `npm run validate` passed locally. Awaiting commit/push/deploy approval and later operator-approved live webhook smoke after Shopier credentials/webhooks are configured.
- _local only, not deployed:_ Phase 7 operator order lifecycle policy tests prepared. `test:order-desk` covers ship/deliver/cancel transition rules, no-write idempotency, missing orders, and manual cancel no-auto-restock policy. `test:order-desk`, `typecheck`, `git diff --check`, and full `npm run validate` passed locally; awaiting commit/push/deploy approval.

## 2026-07-02
- _local only, not deployed:_ manual publish override fix prepared for UygunOps **Yayına Al** semantics. This entry is superseded by D-467: explicit operator approval may bypass generic Image QC/audit review only after protected-brand safety passes; a protected-brand match is never overridable. Hard commerce blockers still apply. Validation passed locally. Awaiting commit/push/deploy approval.

## 2026-06-18
- **D-324** — _production DATA op (no code deploy):_ Admin unpublished placeholder product id 361 `Taslak Ürün 16/06-4184` (`status active → draft`). No git/code change; docs-only commit `docs: record D-324 catalog hygiene`.

## 2026-06-14
- _docs-only commits (no runtime/deploy impact):_ `0f46017` D-308→D-321 project-memory sync; D-322 test-lead-cleanup record (this commit).
- `9a8001b` — **D-320** `/api/inquiries` productId string→number coercion (product-page lead HTTP 500 fix). Deployed; live re-test passed.
- `fd5fbc1` — **D-318** PDP trust-strip claim cleanup ("Hızlı Geri Dönüş/Teslimat" → "WhatsApp Destek / Kargo Süreci").
- `0d004b7` — **D-317** PDP footer dynamic year + removed unsupported "hızlı kargo" claim.
- `c944940` — **D-316A** internal `trackEvent` foundation (no external pixels/scripts).
- `55fad8b` — **D-315** first-touch UTM attribution (sessionStorage) survives homepage→PDP + hero/sticky WhatsApp prefill.
- `003c71f` — **D-314/D-314b** ad-readiness cleanup (external Unsplash removed, duplicate WhyUs removed, About shortened, editorial→gradient, safer tiles, typo, unused-code removal).
- `0899e0f` — **D-313** demo reviews OFF in production.
- `ef5055b` — **D-310/311/312** full-width editorial section + "Tarzına Göre Seç" tiles + social-proof section + premium footer.
- `1e2e862` — **D-304→D-308** Phase 1 conversion upgrade + PDP polish.
- `507608a` — **D-302/D-303** product-card image bg/contain + image-gen studio/74–80%/suede standard.

_Telegram live publishing remains OFF; Instagram feed publishing OFF in AutomationSettings (`publishInstagram=false`)._
